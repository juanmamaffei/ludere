json.extract! goal, :id, :nombre, :descripcion, :finaliza, :video, :pdf, :link1, :link2, :abrevencido, :evaluavencido, :peso, :evaluacion, :created_at, :updated_at
json.url goal_url(goal, format: :json)
